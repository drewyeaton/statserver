Simple Stats Server
===================

Simple Stats Server is a small server that accepts floating point numbers and prints a stats on those numbers.

    # Start the server:
    python statserver/server.py localhost:5050
